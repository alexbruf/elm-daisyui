-- GENERATED by tools/gen-schema.js. Do not edit.


module Daisy.Schema.Select exposing
    ( Color(..), Size(..), Style(..)
    , allColors, allSizes, allStyles, colorToClass, component, componentClasses, parts, sizeToClass, styleToClass
    )

{-| Generated schema for the daisyUI `select` component.

@docs Color, Size, Style

@docs allColors, allSizes, allStyles, colorToClass, component, componentClasses, parts, sizeToClass, styleToClass

-}


{-| The main daisyUI class of the `select` component.
-}
component : String
component =
    "select"


{-| Every class in the `component` group of `select`.
-}
componentClasses : List String
componentClasses =
    [ "select"
    ]


{-| Every class in the `part` group of `select`. A part is only valid
inside an element carrying `component`.
-}
parts : List String
parts =
    []


{-| The `color` group of `select`. At most one value may be applied.
-}
type Color
    = Neutral
    | Primary
    | Secondary
    | Accent
    | Info
    | Success
    | Warning
    | Error


{-| Every [`Color`](#Color) value.
-}
allColors : List Color
allColors =
    [ Neutral
    , Primary
    , Secondary
    , Accent
    , Info
    , Success
    , Warning
    , Error
    ]


{-| The daisyUI class for a [`Color`](#Color).
-}
colorToClass : Color -> String
colorToClass value =
    case value of
        Neutral ->
            "select-neutral"

        Primary ->
            "select-primary"

        Secondary ->
            "select-secondary"

        Accent ->
            "select-accent"

        Info ->
            "select-info"

        Success ->
            "select-success"

        Warning ->
            "select-warning"

        Error ->
            "select-error"


{-| The `style` group of `select`. At most one value may be applied.
-}
type Style
    = Ghost


{-| Every [`Style`](#Style) value.
-}
allStyles : List Style
allStyles =
    [ Ghost
    ]


{-| The daisyUI class for a [`Style`](#Style).
-}
styleToClass : Style -> String
styleToClass value =
    case value of
        Ghost ->
            "select-ghost"


{-| The `size` group of `select`. At most one value may be applied.
-}
type Size
    = Xs
    | Sm
    | Md
    | Lg
    | Xl


{-| Every [`Size`](#Size) value.
-}
allSizes : List Size
allSizes =
    [ Xs
    , Sm
    , Md
    , Lg
    , Xl
    ]


{-| The daisyUI class for a [`Size`](#Size).
-}
sizeToClass : Size -> String
sizeToClass value =
    case value of
        Xs ->
            "select-xs"

        Sm ->
            "select-sm"

        Md ->
            "select-md"

        Lg ->
            "select-lg"

        Xl ->
            "select-xl"
