-- GENERATED by tools/gen-schema.js. Do not edit.


module Daisy.Schema.Checkbox exposing
    ( Color(..), Size(..)
    , allColors, allSizes, colorToClass, component, componentClasses, parts, sizeToClass
    )

{-| Generated schema for the daisyUI `checkbox` component.

@docs Color, Size

@docs allColors, allSizes, colorToClass, component, componentClasses, parts, sizeToClass

-}


{-| The main daisyUI class of the `checkbox` component.
-}
component : String
component =
    "checkbox"


{-| Every class in the `component` group of `checkbox`.
-}
componentClasses : List String
componentClasses =
    [ "checkbox"
    ]


{-| Every class in the `part` group of `checkbox`. A part is only valid
inside an element carrying `component`.
-}
parts : List String
parts =
    []


{-| The `color` group of `checkbox`. At most one value may be applied.
-}
type Color
    = Primary
    | Secondary
    | Accent
    | Neutral
    | Success
    | Warning
    | Info
    | Error


{-| Every [`Color`](#Color) value.
-}
allColors : List Color
allColors =
    [ Primary
    , Secondary
    , Accent
    , Neutral
    , Success
    , Warning
    , Info
    , Error
    ]


{-| The daisyUI class for a [`Color`](#Color).
-}
colorToClass : Color -> String
colorToClass value =
    case value of
        Primary ->
            "checkbox-primary"

        Secondary ->
            "checkbox-secondary"

        Accent ->
            "checkbox-accent"

        Neutral ->
            "checkbox-neutral"

        Success ->
            "checkbox-success"

        Warning ->
            "checkbox-warning"

        Info ->
            "checkbox-info"

        Error ->
            "checkbox-error"


{-| The `size` group of `checkbox`. At most one value may be applied.
-}
type Size
    = Xs
    | Sm
    | Md
    | Lg
    | Xl


{-| Every [`Size`](#Size) value.
-}
allSizes : List Size
allSizes =
    [ Xs
    , Sm
    , Md
    , Lg
    , Xl
    ]


{-| The daisyUI class for a [`Size`](#Size).
-}
sizeToClass : Size -> String
sizeToClass value =
    case value of
        Xs ->
            "checkbox-xs"

        Sm ->
            "checkbox-sm"

        Md ->
            "checkbox-md"

        Lg ->
            "checkbox-lg"

        Xl ->
            "checkbox-xl"
