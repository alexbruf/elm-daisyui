-- GENERATED by tools/gen-schema.js. Do not edit.


module Daisy.Schema.FileInput exposing
    ( Color(..), Size(..), Style(..)
    , allColors, allSizes, allStyles, colorToClass, component, componentClasses, parts, sizeToClass, styleToClass
    )

{-| Generated schema for the daisyUI `file-input` component.

@docs Color, Size, Style

@docs allColors, allSizes, allStyles, colorToClass, component, componentClasses, parts, sizeToClass, styleToClass

-}


{-| The main daisyUI class of the `file-input` component.
-}
component : String
component =
    "file-input"


{-| Every class in the `component` group of `file-input`.
-}
componentClasses : List String
componentClasses =
    [ "file-input"
    ]


{-| Every class in the `part` group of `file-input`. A part is only valid
inside an element carrying `component`.
-}
parts : List String
parts =
    []


{-| The `color` group of `file-input`. At most one value may be applied.
-}
type Color
    = Neutral
    | Primary
    | Secondary
    | Accent
    | Info
    | Success
    | Warning
    | Error


{-| Every [`Color`](#Color) value.
-}
allColors : List Color
allColors =
    [ Neutral
    , Primary
    , Secondary
    , Accent
    , Info
    , Success
    , Warning
    , Error
    ]


{-| The daisyUI class for a [`Color`](#Color).
-}
colorToClass : Color -> String
colorToClass value =
    case value of
        Neutral ->
            "file-input-neutral"

        Primary ->
            "file-input-primary"

        Secondary ->
            "file-input-secondary"

        Accent ->
            "file-input-accent"

        Info ->
            "file-input-info"

        Success ->
            "file-input-success"

        Warning ->
            "file-input-warning"

        Error ->
            "file-input-error"


{-| The `style` group of `file-input`. At most one value may be applied.
-}
type Style
    = Ghost


{-| Every [`Style`](#Style) value.
-}
allStyles : List Style
allStyles =
    [ Ghost
    ]


{-| The daisyUI class for a [`Style`](#Style).
-}
styleToClass : Style -> String
styleToClass value =
    case value of
        Ghost ->
            "file-input-ghost"


{-| The `size` group of `file-input`. At most one value may be applied.
-}
type Size
    = Xs
    | Sm
    | Md
    | Lg
    | Xl


{-| Every [`Size`](#Size) value.
-}
allSizes : List Size
allSizes =
    [ Xs
    , Sm
    , Md
    , Lg
    , Xl
    ]


{-| The daisyUI class for a [`Size`](#Size).
-}
sizeToClass : Size -> String
sizeToClass value =
    case value of
        Xs ->
            "file-input-xs"

        Sm ->
            "file-input-sm"

        Md ->
            "file-input-md"

        Lg ->
            "file-input-lg"

        Xl ->
            "file-input-xl"
