-- GENERATED by tools/gen-schema.js. Do not edit.


module Daisy.Schema.Textarea exposing
    ( Color(..), Size(..), Style(..)
    , allColors, allSizes, allStyles, colorToClass, component, componentClasses, parts, sizeToClass, styleToClass
    )

{-| Generated schema for the daisyUI `textarea` component.

@docs Color, Size, Style

@docs allColors, allSizes, allStyles, colorToClass, component, componentClasses, parts, sizeToClass, styleToClass

-}


{-| The main daisyUI class of the `textarea` component.
-}
component : String
component =
    "textarea"


{-| Every class in the `component` group of `textarea`.
-}
componentClasses : List String
componentClasses =
    [ "textarea"
    ]


{-| Every class in the `part` group of `textarea`. A part is only valid
inside an element carrying `component`.
-}
parts : List String
parts =
    []


{-| The `color` group of `textarea`. At most one value may be applied.
-}
type Color
    = Neutral
    | Primary
    | Secondary
    | Accent
    | Info
    | Success
    | Warning
    | Error


{-| Every [`Color`](#Color) value.
-}
allColors : List Color
allColors =
    [ Neutral
    , Primary
    , Secondary
    , Accent
    , Info
    , Success
    , Warning
    , Error
    ]


{-| The daisyUI class for a [`Color`](#Color).
-}
colorToClass : Color -> String
colorToClass value =
    case value of
        Neutral ->
            "textarea-neutral"

        Primary ->
            "textarea-primary"

        Secondary ->
            "textarea-secondary"

        Accent ->
            "textarea-accent"

        Info ->
            "textarea-info"

        Success ->
            "textarea-success"

        Warning ->
            "textarea-warning"

        Error ->
            "textarea-error"


{-| The `style` group of `textarea`. At most one value may be applied.
-}
type Style
    = Ghost


{-| Every [`Style`](#Style) value.
-}
allStyles : List Style
allStyles =
    [ Ghost
    ]


{-| The daisyUI class for a [`Style`](#Style).
-}
styleToClass : Style -> String
styleToClass value =
    case value of
        Ghost ->
            "textarea-ghost"


{-| The `size` group of `textarea`. At most one value may be applied.
-}
type Size
    = Xs
    | Sm
    | Md
    | Lg
    | Xl


{-| Every [`Size`](#Size) value.
-}
allSizes : List Size
allSizes =
    [ Xs
    , Sm
    , Md
    , Lg
    , Xl
    ]


{-| The daisyUI class for a [`Size`](#Size).
-}
sizeToClass : Size -> String
sizeToClass value =
    case value of
        Xs ->
            "textarea-xs"

        Sm ->
            "textarea-sm"

        Md ->
            "textarea-md"

        Lg ->
            "textarea-lg"

        Xl ->
            "textarea-xl"
